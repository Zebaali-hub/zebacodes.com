import type { Pattern } from '../types'

/** Tier 2 — the Big-Tech layer. Cross-pattern composition starts here. */
export const tier2Patterns: Pattern[] = [
  /* ── Advanced Trees ───────────────────────────────────────── */
  {
    id: 'tree-serialize', topicId: 'advanced-trees', tier: 2,
    title: 'Serialise and reconstruct a tree',
    purpose: 'Encode structure and null positions so a tree survives a round trip through a flat string.',
    subPatterns: ['preorder with null markers', 'level-order encoding', 'construct from preorder + inorder'],
    prerequisites: ['tree-dfs'],
    recognitionSignals: ['"Serialize and deserialize"', 'Construct a tree from one or two traversals', 'A tree must cross a process or network boundary'],
    bruteForceSmell: 'Encoding values only. Without null markers the shape is ambiguous — many trees produce the same value sequence.',
    whyItWorks: 'Preorder plus explicit nulls uniquely determines a binary tree, because the first unconsumed token is always the next node in a deterministic walk.',
    whenNotToUse: ['The tree is a BST — ordering alone reconstructs it without null markers', 'Only the value multiset matters'],
    commonMistakes: ['Omitting null markers and producing an ambiguous encoding', 'Rebuilding with an O(n) index lookup per node, making construction O(n²) instead of using a value→index map'],
    typicalTC: 'O(n)', typicalSC: 'O(n)',
    combinesWith: ['hash-lookup', 'tree-dfs'], googlePriority: true,
  },
  {
    id: 'tree-parent-links', topicId: 'advanced-trees', tier: 2,
    title: 'Treat a tree as a graph',
    purpose: 'Add parent pointers so traversal can move upward, turning a tree problem into a graph problem.',
    subPatterns: ['nodes at distance K', 'burning tree', 'shortest path between two nodes'],
    prerequisites: ['tree-dfs', 'bfs-queue'],
    recognitionSignals: ['"Distance K from a target node" — movement in all directions', 'The answer requires travelling up as well as down'],
    bruteForceSmell: 'Computing distance from the root for every node, then filtering — it misses that the path may go up and back down through an ancestor.',
    whyItWorks: 'With parent links every node has up to three neighbours, so ordinary BFS gives distances in the undirected tree.',
    whenNotToUse: ['Movement is strictly downward', 'The tree is huge and only one query is made — a single DFS with a returned offset is cheaper'],
    commonMistakes: ['Forgetting a visited set and walking back into the parent forever', 'Building parent links lazily inside the BFS instead of in a prior pass'],
    typicalTC: 'O(n)', typicalSC: 'O(n)',
    combinesWith: ['bfs-queue', 'hash-lookup'], googlePriority: true,
  },
  {
    id: 'bst-iterator', topicId: 'advanced-trees', tier: 2,
    title: 'Controlled in-order traversal',
    purpose: 'Pause an in-order walk between nodes by holding the recursion stack explicitly.',
    subPatterns: ['BST iterator', 'kth smallest with early exit', 'predecessor and successor'],
    prerequisites: ['bst-property'],
    recognitionSignals: ['An iterator interface with next() and hasNext()', 'O(h) space demanded rather than O(n)', 'Only the first k values are needed'],
    bruteForceSmell: 'Flattening the whole tree into a list first — O(n) space, and it does the work even if the caller stops after one call.',
    whyItWorks: 'The left spine on an explicit stack is exactly the recursion state an in-order walk would hold at that moment.',
    whenNotToUse: ['The whole traversal is always consumed and memory is free', 'The tree mutates during iteration'],
    commonMistakes: ['Pushing the right subtree at the wrong moment and breaking the ordering', 'Amortised O(1) claimed while re-descending from the root each call'],
    typicalTC: 'O(1) amortised per next()', typicalSC: 'O(h)',
    combinesWith: ['bst-property'], googlePriority: true,
  },
  {
    id: 'tree-dp-intro', topicId: 'advanced-trees', tier: 2,
    title: 'Tree DP — returning state upward',
    purpose: 'Return a small tuple from each subtree so the parent can decide without re-walking.',
    subPatterns: ['max path sum', 'house robber on a tree', 'longest univalue path'],
    prerequisites: ['tree-dfs', 'dp-1d'],
    recognitionSignals: ['The answer at a node depends on a CHOICE made in its children', '"Maximum path", "cannot use adjacent nodes"'],
    bruteForceSmell: 'Recomputing subtree answers for every starting node — O(n²) when one post-order pass suffices.',
    whyItWorks: 'Each node returns the best value usable BY THE PARENT, while separately recording the best answer that passes THROUGH it. Those are different quantities.',
    whenNotToUse: ['State depends on the whole tree rather than a subtree — consider rerooting'],
    commonMistakes: [
      'Returning the through-path instead of the upward-usable value — the single most common Tree DP bug',
      'Allowing negative contributions instead of clamping them to zero',
    ],
    typicalTC: 'O(n)', typicalSC: 'O(h)',
    combinesWith: ['tree-dfs', 'dp-1d'], googlePriority: true,
  },

  /* ── Advanced Graphs ──────────────────────────────────────── */
  {
    id: 'topological-sort', topicId: 'advanced-graphs', tier: 2,
    title: 'Topological sort',
    purpose: 'Order a directed acyclic graph so every edge points forward, and detect cycles while doing it.',
    subPatterns: ['Kahn indegree BFS', 'DFS post-order reversal', 'course schedule', 'alien dictionary'],
    prerequisites: ['graph-traversal'],
    recognitionSignals: ['Dependencies, prerequisites, build order', '"Can all tasks be finished" — a cycle question in disguise', 'An ordering must respect constraints'],
    bruteForceSmell: 'Repeatedly scanning for a node with no unmet dependencies — O(V²) when indegree counting is O(V + E).',
    whyItWorks: 'A node with indegree zero has no unmet dependency, so emitting it can never violate an edge. If the queue empties early, the remainder contains a cycle.',
    whenNotToUse: ['The graph is undirected', 'The graph has cycles by design'],
    commonMistakes: ['Not comparing the emitted count against V, so a cycle is reported as a valid order', 'Building edges in the wrong direction'],
    typicalTC: 'O(V + E)', typicalSC: 'O(V + E)',
    combinesWith: ['graph-traversal', 'dag-dp'], googlePriority: true,
  },
  {
    id: 'dsu', topicId: 'advanced-graphs', tier: 2,
    title: 'Disjoint set union',
    purpose: 'Answer connectivity under incremental merges in near-constant amortised time.',
    subPatterns: ['path compression', 'union by rank or size', 'component counting', 'cycle detection in undirected graphs'],
    prerequisites: ['graph-traversal'],
    recognitionSignals: ['Edges arrive over time and connectivity is queried repeatedly', '"Number of provinces", "redundant connection", "accounts merge"', 'Kruskal MST'],
    bruteForceSmell: 'Re-running BFS after every merge — O(V + E) per query, when the merges are incremental and never split.',
    whyItWorks: 'Path compression flattens the tree during find, and union by rank keeps it shallow; together they give inverse-Ackermann amortised cost.',
    whenNotToUse: ['Connections are removed as well as added — DSU cannot split', 'A single traversal answers the question once'],
    commonMistakes: ['Omitting path compression and degrading to O(n) per find', 'Comparing roots by value instead of by find() result'],
    typicalTC: 'O(α(n)) amortised per operation', typicalSC: 'O(n)',
    combinesWith: ['sorting-greedy', 'graph-traversal'], googlePriority: true,
  },
  {
    id: 'dijkstra', topicId: 'advanced-graphs', tier: 2,
    title: 'Dijkstra — greedy shortest path with a heap',
    purpose: 'Find shortest paths on non-negative weighted edges by always expanding the nearest unsettled node.',
    subPatterns: ['network delay', 'cheapest path with constraints', 'path with maximum probability', 'swim in rising water'],
    prerequisites: ['bfs-queue', 'heap-topk'],
    recognitionSignals: ['Weighted edges with non-negative costs', '"Minimum time", "cheapest", "least effort"', 'BFS would be correct if only the weights were uniform'],
    bruteForceSmell: 'BFS on a weighted graph — it counts hops, not cost, and silently returns the wrong answer.',
    whyItWorks: 'With non-negative weights the nearest unsettled node can never be improved later, so its distance can be finalised on pop.',
    whenNotToUse: ['Negative edges exist — use Bellman-Ford', 'All weights are equal — plain BFS is faster', 'Weights are only 0 and 1 — use 0-1 BFS'],
    commonMistakes: ['Marking visited on push rather than on pop', 'Not skipping a stale heap entry whose distance is worse than the recorded one'],
    typicalTC: 'O(E log V)', typicalSC: 'O(V + E)',
    combinesWith: ['heap-topk', 'graph-traversal'], googlePriority: true,
  },
  {
    id: 'graph-state-space', topicId: 'advanced-graphs', tier: 2,
    title: 'State-space search',
    purpose: 'Make the node a tuple of position and carried state, then run ordinary BFS over it.',
    subPatterns: ['keys and locks', 'k obstacle eliminations', 'bipartite two-colouring', 'word ladder'],
    prerequisites: ['bfs-queue', 'graph-traversal'],
    recognitionSignals: ['Position alone is not enough to decide whether a move is legal', '"At most k removals", "collect all keys"', 'Revisiting a cell is allowed in a different state'],
    bruteForceSmell: 'Visiting by position only — it blocks a genuinely better path that arrives in a different state.',
    whyItWorks: 'Expanding the node identity to (position, state) restores the property BFS needs: first arrival at a node is optimal.',
    whenNotToUse: ['The state space explodes beyond memory', 'State does not affect legality'],
    commonMistakes: ['A visited set keyed on position instead of the full state tuple', 'Bitmask width mismatched to the number of keys'],
    typicalTC: 'O(V · S + E · S)', typicalSC: 'O(V · S)',
    combinesWith: ['bfs-queue', 'bitmask'], googlePriority: true,
  },

  /* ── Dynamic Programming ──────────────────────────────────── */
  {
    id: 'dp-knapsack', topicId: 'dynamic-programming', tier: 2,
    title: 'Knapsack — bounded and unbounded',
    purpose: 'Decide take-or-skip over items with a capacity, and know which reuse rule changes the loop order.',
    subPatterns: ['0/1 knapsack', 'unbounded knapsack', 'partition equal subset', 'coin change II', 'target sum'],
    prerequisites: ['dp-1d'],
    recognitionSignals: ['A budget, capacity or target, plus a set of items', '"Can we reach exactly", "how many ways", "minimum coins"'],
    bruteForceSmell: 'Enumerating every subset — 2^n, with the same (index, remaining) pair recomputed enormously often.',
    whyItWorks: 'State is (index, remaining capacity). Each item is a binary choice, so the table has n·C entries and each is filled once.',
    whenNotToUse: ['Capacity is huge and unbounded — the table does not fit', 'A greedy choice is provably safe, as with canonical coin systems'],
    commonMistakes: [
      'Iterating capacity ascending for 0/1 and accidentally reusing an item — 0/1 needs descending on a 1D array',
      'Confusing counting order: permutations need the item loop inside, combinations need it outside',
    ],
    typicalTC: 'O(n·C)', typicalSC: 'O(C) after space optimisation',
    combinesWith: ['dp-1d'], googlePriority: true,
  },
  {
    id: 'dp-lcs', topicId: 'dynamic-programming', tier: 2,
    title: 'Two-sequence DP',
    purpose: 'Compare two strings by indexing state on both, matching or advancing one side.',
    subPatterns: ['longest common subsequence', 'edit distance', 'distinct subsequences', 'interleaving string'],
    prerequisites: ['dp-grid'],
    recognitionSignals: ['Two sequences compared', '"Edit", "transform", "common subsequence", "how many ways to form"'],
    bruteForceSmell: 'Recursing on both indices without memoisation — exponential, with heavy overlap on (i, j).',
    whyItWorks: 'Characters either match, consuming both, or do not, branching on which side advances. Both are O(1) transitions over an m·n table.',
    whenNotToUse: ['The subsequence must be contiguous — that is a different, simpler recurrence'],
    commonMistakes: ['Base row and column initialised to zero when edit distance needs them to be the index', 'Off-by-one between string index and table index'],
    typicalTC: 'O(m·n)', typicalSC: 'O(m·n), reducible to O(min(m,n))',
    combinesWith: ['dp-grid'], googlePriority: true,
  },
  {
    id: 'dp-lis', topicId: 'dynamic-programming', tier: 2,
    title: 'Longest increasing subsequence',
    purpose: 'Track the best tail for each achievable length, and binary search it for the O(n log n) form.',
    subPatterns: ['LIS O(n²)', 'LIS with patience sorting', 'russian doll envelopes', 'longest chain'],
    prerequisites: ['dp-1d', 'binary-search-bounds'],
    recognitionSignals: ['"Longest increasing", "maximum chain", "envelopes that fit inside"', 'Order matters and the subsequence need not be contiguous'],
    bruteForceSmell: 'Comparing every pair for the O(n²) form — acceptable at small n, and explicitly ruled out when O(n log n) is demanded.',
    whyItWorks: 'The tails array holds the smallest possible tail for each length. It stays sorted, so the replacement position is binary searchable.',
    whenNotToUse: ['The subsequence must be contiguous', 'n is small and clarity matters more'],
    commonMistakes: [
      'Believing the tails array IS the subsequence — it has the right LENGTH but not necessarily valid contents',
      'Using lower vs upper bound wrongly and breaking strict versus non-strict increase',
    ],
    typicalTC: 'O(n log n)', typicalSC: 'O(n)',
    combinesWith: ['binary-search-bounds', 'sorting-greedy'], googlePriority: true,
  },
  {
    id: 'dp-state-machine', topicId: 'dynamic-programming', tier: 2,
    title: 'State-machine DP',
    purpose: 'Name the states a process can be in, then write one transition per legal move.',
    subPatterns: ['stock with cooldown', 'stock with fee', 'at most k transactions', 'paint house'],
    prerequisites: ['dp-1d'],
    recognitionSignals: ['Buy, sell, hold, cooldown — a small set of modes', 'A constraint couples consecutive decisions'],
    bruteForceSmell: 'Trying every buy/sell pairing — exponential, and it hides that only a handful of states matter at each step.',
    whyItWorks: 'The future depends only on which state you are in, not how you got there, so a constant number of running values suffices.',
    whenNotToUse: ['States are unbounded', 'No constraint links consecutive choices'],
    commonMistakes: ['Updating states in an order that lets one transition read another already updated this step', 'Wrong initial values for unreachable states'],
    typicalTC: 'O(n · states)', typicalSC: 'O(states)',
    combinesWith: ['dp-1d'], googlePriority: true,
  },

  /* ── Binary Search on Answer ──────────────────────────────── */
  {
    id: 'binary-search-answer', topicId: 'binary-search-answer', tier: 2,
    title: 'Search the answer space with a feasibility check',
    purpose: 'When the answer is monotone, binary search the ANSWER and verify each guess in linear time.',
    subPatterns: ['minimise the maximum', 'maximise the minimum', 'capacity allocation', 'koko eating bananas'],
    prerequisites: ['binary-search-bounds'],
    recognitionSignals: [
      'The answer is a NUMBER in a known range, not an index',
      '"Minimum largest", "maximum smallest", "smallest capacity such that"',
      'Checking a candidate is easy; producing the best directly is not',
    ],
    bruteForceSmell: 'Trying every candidate value and testing each — linear in the value range, which is usually far larger than n.',
    whyItWorks: 'Feasibility is monotone: if capacity c works, every c larger works too. That step function is exactly what binary search needs.',
    whenNotToUse: ['Feasibility is not monotone in the candidate', 'The answer is not numeric'],
    commonMistakes: [
      'Choosing the wrong initial bounds — lo must be a value that could be feasible, hi one that certainly is',
      'Writing a feasibility function that is subtly non-monotone',
    ],
    typicalTC: 'O(n log(range))', typicalSC: 'O(1)',
    combinesWith: ['sorting-greedy', 'binary-search-bounds'], googlePriority: true,
  },

  /* ── Monotonic stack / deque (Tier 2 depth) ───────────────── */
  {
    id: 'monotonic-contribution', topicId: 'monotonic', tier: 2,
    title: 'Contribution counting with a monotonic stack',
    purpose: 'Count, for each element, how many subarrays it dominates, by finding its span between strictly greater neighbours.',
    subPatterns: ['largest rectangle in histogram', 'sum of subarray minimums', 'maximal rectangle'],
    prerequisites: ['monotonic-stack'],
    recognitionSignals: ['Sum or count over ALL subarrays where an element is the min or max', 'Histogram or skyline shapes'],
    bruteForceSmell: 'Enumerating every subarray and taking its minimum — O(n²) or O(n³), when each element contributes over a computable span.',
    whyItWorks: 'The previous-smaller and next-smaller indices bound exactly the subarrays in which the element is the minimum, so its total contribution is a product of two distances.',
    whenNotToUse: ['The aggregate is not min or max', 'Subarrays are bounded in length — use a window'],
    commonMistakes: [
      'Identical values double-counted — one side must use strict comparison and the other non-strict',
      'Not flushing the stack at the end with a sentinel',
    ],
    typicalTC: 'O(n)', typicalSC: 'O(n)',
    combinesWith: ['monotonic-stack'], googlePriority: true,
  },

  /* ── Advanced heap ────────────────────────────────────────── */
  {
    id: 'two-heaps', topicId: 'advanced-heap', tier: 2,
    title: 'Two heaps for a running median',
    purpose: 'Split a stream into a max-heap of the lower half and a min-heap of the upper half.',
    subPatterns: ['find median from data stream', 'sliding window median', 'IPO / capital scheduling'],
    prerequisites: ['heap-topk'],
    recognitionSignals: ['A median or middle value over a stream', 'Insertions arrive continuously and queries interleave'],
    bruteForceSmell: 'Re-sorting after every insertion — O(n log n) per query on data that changes by one element.',
    whyItWorks: 'Keeping the halves balanced within one element puts the median at one or both roots, so a query is O(1) and an insertion O(log n).',
    whenNotToUse: ['The full dataset is available upfront — sort once', 'Arbitrary deletions are needed — heaps delete lazily at best'],
    commonMistakes: ['Rebalancing before inserting rather than after', 'Integer overflow when averaging the two roots'],
    typicalTC: 'O(log n) insert, O(1) query', typicalSC: 'O(n)',
    combinesWith: ['heap-topk'], googlePriority: true,
  },
  {
    id: 'heap-scheduling', topicId: 'advanced-heap', tier: 2,
    title: 'Heap-driven scheduling and merging',
    purpose: 'Always process the next-available item by keeping candidates in a priority queue.',
    subPatterns: ['task scheduler', 'meeting rooms II', 'merge k sorted lists', 'reorganise string'],
    prerequisites: ['heap-topk', 'intervals-merge'],
    recognitionSignals: ['"Minimum number of rooms", "least intervals", "merge k sorted"', 'Repeatedly needing the smallest or most frequent remaining item'],
    bruteForceSmell: 'Rescanning all candidates each step to find the next one — O(n·k) where a heap gives O(n log k).',
    whyItWorks: 'The heap invariant makes the next choice O(log k) and, for interval problems, the earliest end time is the only thing that can free capacity.',
    whenNotToUse: ['Candidates are already sorted and consumed in order', 'k is tiny — a linear scan is cheaper than heap overhead'],
    commonMistakes: ['Pushing all items instead of only the frontier when merging k lists', 'Comparator on the wrong field for interval rooms'],
    typicalTC: 'O(n log k)', typicalSC: 'O(k)',
    combinesWith: ['intervals-merge', 'hash-frequency'], googlePriority: true,
  },

  /* ── Advanced backtracking ────────────────────────────────── */
  {
    id: 'backtracking-pruning', topicId: 'advanced-backtracking', tier: 2,
    title: 'Pruning and duplicate handling',
    purpose: 'Cut branches that cannot lead to a solution, and skip equal siblings so results stay unique.',
    subPatterns: ['subsets II', 'combination sum II', 'palindrome partitioning', 'N-queens', 'sudoku'],
    prerequisites: ['backtracking-subsets'],
    recognitionSignals: ['The input contains duplicates and the output must not', 'A constraint can be checked before recursing', 'Naive enumeration times out'],
    bruteForceSmell: 'Generating everything and deduplicating at the end — the wasted branches are the cost, and a set does not recover the time.',
    whyItWorks: 'Sorting brings equal values adjacent, so skipping a sibling equal to the previous one removes exactly the duplicate branches, once.',
    whenNotToUse: ['Duplicates are meaningful in the output', 'No constraint is checkable early'],
    commonMistakes: [
      'Skipping when the value equals the previous WITHOUT the i > start guard, which wrongly removes valid uses within one branch',
      'Checking validity after recursing instead of before',
    ],
    typicalTC: 'Exponential, but heavily reduced', typicalSC: 'O(n) depth',
    combinesWith: ['backtracking-subsets', 'sorting-greedy'], googlePriority: true,
  },

  /* ── Trie ─────────────────────────────────────────────────── */
  {
    id: 'trie-prefix', topicId: 'trie', tier: 2,
    title: 'Trie for shared prefixes',
    purpose: 'Store a dictionary as a character tree so prefix queries cost the length of the prefix, not the size of the set.',
    subPatterns: ['implement trie', 'wildcard search', 'word search II', 'autocomplete', 'maximum XOR pair'],
    prerequisites: ['tree-dfs', 'hash-lookup'],
    recognitionSignals: ['Many words share prefixes', '"startsWith", wildcard matching, autocomplete', 'A word list is searched repeatedly against a grid or stream'],
    bruteForceSmell: 'Checking every word independently — O(words · length) per query, re-walking prefixes the words share.',
    whyItWorks: 'Shared prefixes are stored once, so a search descends a single path and dead ends are discovered as soon as a child is missing.',
    whenNotToUse: ['Only exact lookups are needed — a hash set is simpler and faster', 'The alphabet is huge and words are few'],
    commonMistakes: [
      'No end-of-word flag, so a prefix is reported as a complete word',
      'In word search II, not pruning exhausted trie branches and losing the whole benefit',
    ],
    typicalTC: 'O(L) per operation', typicalSC: 'O(total characters)',
    combinesWith: ['backtracking-grid', 'bitmask'], googlePriority: true,
  },
]
