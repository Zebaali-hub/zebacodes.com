import type { Pattern } from '../types'

/**
 * Tier 1 patterns.
 *
 * Every field here exists to teach RECOGNITION, not definition.
 * `recognitionSignals` comes first in the UI for that reason: knowing
 * what a sliding window is has never been the hard part of an interview.
 */
export const tier1Patterns: Pattern[] = [
  /* ── Arrays & Strings ─────────────────────────────────────── */
  {
    id: 'array-traversal', topicId: 'arrays-strings', tier: 1,
    title: 'Single-pass traversal with running state',
    purpose: 'Carry a small amount of state across one pass instead of re-scanning the array.',
    subPatterns: ['running max/min', 'running sum', 'best-so-far', 'index of best-so-far'],
    prerequisites: [],
    recognitionSignals: [
      'The answer is a single value derived from the whole array',
      'A naive solution compares every element with every other element',
      'Phrases like "maximum profit", "best time", "largest so far"',
    ],
    bruteForceSmell: 'Nested loops comparing every pair — the inner loop only ever needs the best value seen so far, not every value.',
    whyItWorks: 'The quantity you need from the prefix is summarisable in O(1) state, so the prefix never has to be revisited.',
    whenNotToUse: ['The answer depends on a window of bounded size — use sliding window', 'You need every pair, not the best pair'],
    commonMistakes: ['Initialising the running best to 0 instead of the first element or -Infinity', 'Updating the answer before updating the running state, or vice versa, in the wrong order'],
    typicalTC: 'O(n)', typicalSC: 'O(1)',
    combinesWith: ['hash-frequency', 'prefix-sum'], googlePriority: false,
  },
  {
    id: 'string-frequency', topicId: 'arrays-strings', tier: 1,
    title: 'Character frequency comparison',
    purpose: 'Decide equivalence or grouping by counting characters rather than comparing arrangements.',
    subPatterns: ['anagram check', 'frequency signature', 'fixed-alphabet counting array'],
    prerequisites: ['hash-frequency'],
    recognitionSignals: [
      'Order does not matter, only composition',
      'Words like "anagram", "permutation of", "rearrange to form"',
      'A bounded alphabet (26 lowercase letters) is stated in constraints',
    ],
    bruteForceSmell: 'Sorting both strings to compare — O(n log n) when counting is O(n), and sorting throws away the counts you actually want.',
    whyItWorks: 'A multiset of characters is fully described by its counts, so two strings are anagrams exactly when their count vectors are equal.',
    whenNotToUse: ['Order matters', 'The alphabet is unbounded and only one comparison is needed — sorting may be simpler'],
    commonMistakes: ['Comparing lengths last instead of first', 'Using a 26-slot array when the input can contain Unicode'],
    typicalTC: 'O(n)', typicalSC: 'O(1) for a fixed alphabet, O(k) otherwise',
    combinesWith: ['hash-grouping', 'sliding-window-frequency'], googlePriority: false,
  },
  {
    id: 'matrix-traversal', topicId: 'arrays-strings', tier: 1,
    title: 'Matrix traversal and in-place transformation',
    purpose: 'Walk or mutate a 2D grid without allocating a second grid.',
    subPatterns: ['row/column sweep', 'transpose then reverse', 'boundary spiral', 'first-row/column as marker storage'],
    prerequisites: ['array-traversal'],
    recognitionSignals: ['An m × n grid with an O(1) extra space requirement', 'Rotation, spiral order, or zeroing rows and columns'],
    bruteForceSmell: 'Copying into a fresh matrix — correct, but forfeits the constraint the question is actually testing.',
    whyItWorks: 'A rotation decomposes into a transpose plus a row reversal; marker storage reuses cells whose values are already determined.',
    whenNotToUse: ['The input must not be mutated', 'The grid is a graph in disguise — use BFS/DFS'],
    commonMistakes: ['Looping the full matrix during transpose and undoing the swap', 'Losing the first row/column flags when using them as markers'],
    typicalTC: 'O(m·n)', typicalSC: 'O(1)',
    combinesWith: ['graph-grid'], googlePriority: false,
  },

  /* ── Hashing ──────────────────────────────────────────────── */
  {
    id: 'hash-lookup', topicId: 'hashing', tier: 1,
    title: 'Complement lookup',
    purpose: 'Trade space for time by remembering what you have seen so the match can be found in O(1).',
    subPatterns: ['two-sum complement', 'seen-set membership', 'value to index map'],
    prerequisites: [],
    recognitionSignals: [
      'You are searching for a partner element that satisfies an equation',
      'Constraints allow O(n) space but forbid O(n²) time',
      '"Return the indices" rather than the values — the map must store positions',
    ],
    bruteForceSmell: 'Two nested loops asking "does a partner exist?" — the inner loop is a membership query, and membership queries belong in a hash set.',
    whyItWorks: 'Rearranging the condition isolates the partner, so it can be looked up directly instead of searched for.',
    whenNotToUse: ['The array is sorted and O(1) space is required — use two pointers', 'You need all pairs, not existence'],
    commonMistakes: ['Inserting the current element before checking, so it matches itself', 'Returning values when the question asked for indices'],
    typicalTC: 'O(n)', typicalSC: 'O(n)',
    combinesWith: ['two-pointers-sorted', 'prefix-sum'], googlePriority: true,
  },
  {
    id: 'hash-frequency', topicId: 'hashing', tier: 1,
    title: 'Frequency counting',
    purpose: 'Reduce a collection to counts, then answer questions about the counts rather than the collection.',
    subPatterns: ['count map', 'duplicate detection', 'majority reasoning', 'count then bucket'],
    prerequisites: [],
    recognitionSignals: ['"How many times", "most frequent", "appears more than"', 'Duplicates matter but positions do not'],
    bruteForceSmell: 'Counting occurrences of each element by re-scanning the array for it — O(n²) to build information one pass can build.',
    whyItWorks: 'Counting is a fold over the input; every count question is then a query over a much smaller map.',
    whenNotToUse: ['Only presence matters, not counts — a set is cheaper', 'Memory is the binding constraint'],
    commonMistakes: ['Mutating the map while iterating it', 'Forgetting that ties exist when picking the maximum'],
    typicalTC: 'O(n)', typicalSC: 'O(k) distinct values',
    combinesWith: ['heap-topk', 'sliding-window-frequency', 'bucket-sort'], googlePriority: true,
  },
  {
    id: 'hash-grouping', topicId: 'hashing', tier: 1,
    title: 'Grouping by computed key',
    purpose: 'Partition items by deriving a canonical key that collides exactly when items belong together.',
    subPatterns: ['sorted-string key', 'count-signature key', 'normalised-form key'],
    prerequisites: ['hash-frequency'],
    recognitionSignals: ['"Group all", "bucket by", "find all sets of"', 'Two items belong together under an equivalence relation'],
    bruteForceSmell: 'Comparing every item against every group representative — O(n²·k) when a key computes in O(k).',
    whyItWorks: 'A well-chosen key is a canonical form: equal keys and equivalent items are the same statement.',
    whenNotToUse: ['The relation is not an equivalence — use union-find', 'Keys are expensive to compute and groups are few'],
    commonMistakes: ['Choosing a key that collides for non-equivalent items', 'Using a mutable array as a map key'],
    typicalTC: 'O(n·k)', typicalSC: 'O(n·k)',
    combinesWith: ['string-frequency', 'dsu'], googlePriority: true,
  },
  {
    id: 'hash-sequence', topicId: 'hashing', tier: 1,
    title: 'Set membership for sequence reconstruction',
    purpose: 'Use O(1) membership to walk a sequence that is scattered through unsorted data.',
    subPatterns: ['longest consecutive run', 'start-of-sequence detection'],
    prerequisites: ['hash-lookup'],
    recognitionSignals: ['"Consecutive", "longest run", and an explicit O(n) requirement that rules out sorting'],
    bruteForceSmell: 'Sorting first — O(n log n), and the question usually says O(n) precisely to block it.',
    whyItWorks: 'Only starting elements (those with no predecessor in the set) begin a run, so each element is visited a bounded number of times.',
    whenNotToUse: ['Sorting is acceptable and clarity matters more'],
    commonMistakes: ['Walking a run from every element, making it quadratic', 'Not deduplicating before counting'],
    typicalTC: 'O(n)', typicalSC: 'O(n)',
    combinesWith: ['dsu'], googlePriority: true,
  },

  /* ── Two Pointers ─────────────────────────────────────────── */
  {
    id: 'two-pointers-sorted', topicId: 'two-pointers', tier: 1,
    title: 'Opposite-direction pointers on sorted data',
    purpose: 'Converge from both ends, discarding half the remaining possibilities at each step.',
    subPatterns: ['pair sum', 'container width/height trade', 'palindrome check', '3Sum outer loop'],
    prerequisites: [],
    recognitionSignals: [
      'The array is sorted, or sorting does not destroy the answer',
      'O(1) extra space is demanded',
      'The objective is monotone in the direction each pointer moves',
    ],
    bruteForceSmell: 'Every pair in nested loops — but on sorted data the comparison tells you which side can never improve.',
    whyItWorks: 'At each step one pointer is provably not part of any better answer, so it can be discarded without checking its remaining pairs.',
    whenNotToUse: ['The data cannot be sorted because indices must be preserved', 'The objective is not monotone in pointer movement'],
    commonMistakes: ['Forgetting to skip duplicates in 3Sum, producing repeated triples', 'Moving the wrong pointer when the objective is width-limited rather than value-limited'],
    typicalTC: 'O(n) after an O(n log n) sort', typicalSC: 'O(1)',
    combinesWith: ['sorting-greedy', 'hash-lookup'], googlePriority: true,
  },
  {
    id: 'two-pointers-fast-slow', topicId: 'two-pointers', tier: 1,
    title: 'Fast and slow pointers',
    purpose: 'Detect cycles and find midpoints with two pointers moving at different rates.',
    subPatterns: ['cycle detection', 'cycle entry point', 'middle node', 'nth from end'],
    prerequisites: [],
    recognitionSignals: ['A linked structure with no length given', 'O(1) space required', '"Detect a loop", "find the middle", "nth from the end"'],
    bruteForceSmell: 'A visited set — correct but O(n) space, and the constraint usually forbids it.',
    whyItWorks: 'In a cycle the fast pointer gains one position per step on the slow one, so it must eventually land on it; the meeting point has a fixed algebraic relation to the entry.',
    whenNotToUse: ['The structure is a tree or DAG where cycles are impossible', 'You need the cycle length only — a counter is simpler'],
    commonMistakes: ['Dereferencing fast.next without null-checking fast first', 'Starting both pointers at different nodes and breaking the entry-point maths'],
    typicalTC: 'O(n)', typicalSC: 'O(1)',
    combinesWith: ['linked-list-reverse'], googlePriority: true,
  },
  {
    id: 'two-pointers-same-direction', topicId: 'two-pointers', tier: 1,
    title: 'Same-direction read and write pointers',
    purpose: 'Compact or partition an array in place with a slow write pointer trailing a fast read pointer.',
    subPatterns: ['in-place dedupe', 'remove element', 'move zeroes', 'Dutch national flag partition'],
    prerequisites: [],
    recognitionSignals: ['"In place", "return the new length", "do not allocate another array"'],
    bruteForceSmell: 'Building a filtered copy — trivially correct, and exactly what the O(1) space constraint forbids.',
    whyItWorks: 'Everything before the write pointer is already final, so writing there can never destroy data the read pointer still needs.',
    whenNotToUse: ['Order must be preserved and the operation cannot preserve it', 'The original array is needed afterwards'],
    commonMistakes: ['Returning the write index off by one', 'Swapping when overwriting was correct, or the reverse'],
    typicalTC: 'O(n)', typicalSC: 'O(1)',
    combinesWith: ['sorting-greedy'], googlePriority: false,
  },

  /* ── Prefix / Suffix ──────────────────────────────────────── */
  {
    id: 'prefix-sum', topicId: 'prefix-suffix', tier: 1,
    title: 'Prefix sums and range queries',
    purpose: 'Precompute cumulative totals so any range sum answers in O(1).',
    subPatterns: ['1D prefix', '2D prefix', 'suffix product', 'difference array'],
    prerequisites: ['array-traversal'],
    recognitionSignals: ['Many range-sum queries over static data', '"Product of all except self", "sum between i and j"', 'Repeated recomputation of overlapping ranges'],
    bruteForceSmell: 'Summing each range on demand — O(n) per query, and the ranges overlap heavily.',
    whyItWorks: 'sum(i..j) = prefix[j+1] − prefix[i], so every range decomposes into two already-computed values.',
    whenNotToUse: ['The array is mutated between queries — use a Fenwick or segment tree', 'Only one query is made'],
    commonMistakes: ['Off-by-one from mixing inclusive and exclusive bounds', 'Overflow on large sums', 'Using division for product-except-self when zeros are possible'],
    typicalTC: 'O(n) build, O(1) query', typicalSC: 'O(n)',
    combinesWith: ['hash-lookup', 'binary-search-bounds'], googlePriority: true,
  },
  {
    id: 'prefix-hashmap', topicId: 'prefix-suffix', tier: 1,
    title: 'Prefix sum with a hash map',
    purpose: 'Count or locate subarrays with a target property by remembering prefix values already seen.',
    subPatterns: ['subarray sum equals k', 'prefix XOR', 'longest subarray with sum k', 'divisible-by-k remainder map'],
    prerequisites: ['prefix-sum', 'hash-lookup'],
    recognitionSignals: [
      'Count or find SUBARRAYS, not elements',
      'The array contains negatives, which rules out sliding window',
      'A target sum, XOR, or remainder is specified',
    ],
    bruteForceSmell: 'Every (i, j) pair with a running sum — O(n²), and the inner loop is asking a membership question again.',
    whyItWorks: 'A subarray sums to k exactly when prefix[j] − prefix[i] = k, so the search becomes a complement lookup over prefixes.',
    whenNotToUse: ['All values are positive and the property is monotone — sliding window is O(1) space', 'The subarray must be contiguous in a way prefix cannot express'],
    commonMistakes: ['Forgetting to seed the map with prefix 0 → count 1', 'Counting occurrences instead of storing earliest index when asking for longest'],
    typicalTC: 'O(n)', typicalSC: 'O(n)',
    combinesWith: ['hash-frequency'], googlePriority: true,
  },

  /* ── Binary Search ────────────────────────────────────────── */
  {
    id: 'binary-search-bounds', topicId: 'binary-search', tier: 1,
    title: 'Binary search with bound discipline',
    purpose: 'Halve a sorted search space, using a consistent invariant for first and last occurrence.',
    subPatterns: ['exact match', 'first occurrence', 'last occurrence', 'lower bound', 'upper bound', 'insert position'],
    prerequisites: [],
    recognitionSignals: ['Sorted input, or a monotone predicate over the index', 'A required complexity of O(log n)'],
    bruteForceSmell: 'A linear scan — fine at small n, and explicitly excluded once O(log n) is stated.',
    whyItWorks: 'The predicate is monotone, so one side of the midpoint provably cannot contain the boundary.',
    whenNotToUse: ['Data is unsorted and sorting costs more than scanning', 'The predicate is not monotone'],
    commonMistakes: ['Writing (lo + hi) / 2 and overflowing in fixed-width languages', 'Mixing inclusive and exclusive hi across branches and looping forever', 'Returning lo when the invariant makes hi correct'],
    typicalTC: 'O(log n)', typicalSC: 'O(1)',
    combinesWith: ['prefix-sum', 'dp-lis'], googlePriority: true,
  },
  {
    id: 'binary-search-rotated', topicId: 'binary-search', tier: 1,
    title: 'Binary search on a rotated array',
    purpose: 'Search a sorted array that has been rotated, by identifying which half is still sorted.',
    subPatterns: ['find minimum', 'search target', 'find rotation index'],
    prerequisites: ['binary-search-bounds'],
    recognitionSignals: ['"Sorted and then rotated"', 'O(log n) demanded on data that is only locally sorted'],
    bruteForceSmell: 'Linear scan for the pivot, then binary search — the pivot itself is findable in log time.',
    whyItWorks: 'At any midpoint at least one half is fully sorted; comparing the target against that half decides which side to keep.',
    whenNotToUse: ['Duplicates make the sorted-half test ambiguous and degrade it to O(n)'],
    commonMistakes: ['Comparing against the wrong endpoint when deciding which half is sorted', 'Not handling the non-rotated case'],
    typicalTC: 'O(log n)', typicalSC: 'O(1)',
    combinesWith: [], googlePriority: true,
  },
]
