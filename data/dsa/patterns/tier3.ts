import type { Pattern } from '../types'

/** Tier 3 — top-end reasoning. Depth over breadth, deliberately not CP trivia. */
export const tier3Patterns: Pattern[] = [
  /* ── Advanced DP ──────────────────────────────────────────── */
  {
    id: 'dp-interval', topicId: 'advanced-dp', tier: 3,
    title: 'Interval and partition DP',
    purpose: 'Define state over a RANGE and split it at every interior point, choosing the best split.',
    subPatterns: ['burst balloons', 'matrix chain order', 'palindrome partitioning II', 'stone game'],
    prerequisites: ['dp-lcs'],
    recognitionSignals: [
      'The answer for a range depends on choosing a split or a LAST action inside it',
      'Removing an element changes its neighbours — order of operations matters',
      'n is small enough for O(n³)',
    ],
    bruteForceSmell: 'Trying every order of operations — factorial, with the same subrange re-solved constantly.',
    whyItWorks: 'Fixing which element is handled LAST in a range makes the left and right subranges independent, which is what restores optimal substructure.',
    whenNotToUse: ['Ranges do not decompose independently', 'n is large — O(n³) will not fit'],
    commonMistakes: [
      'Thinking about which balloon bursts FIRST rather than LAST — first does not decompose',
      'Iterating in an order where a subrange is read before it is computed; length must be the outer loop',
    ],
    typicalTC: 'O(n³)', typicalSC: 'O(n²)',
    combinesWith: ['dp-lcs'], googlePriority: true,
  },
  {
    id: 'dp-bitmask', topicId: 'advanced-dp', tier: 3,
    title: 'Bitmask DP',
    purpose: 'Use an integer as a set, so "which subset have I used" becomes an O(1) state dimension.',
    subPatterns: ['travelling salesman', 'assignment problems', 'partition to k equal subsets', 'shortest path visiting all nodes'],
    prerequisites: ['dp-1d', 'bitmask'],
    recognitionSignals: ['n is roughly 20 or fewer — the giveaway constraint', '"Visit all", "assign every task", "cover every node"', 'The state genuinely needs a subset, not a count'],
    bruteForceSmell: 'Permuting all n items — n! at n = 15 is a trillion, while 2^n · n is a few million.',
    whyItWorks: 'A bitmask enumerates subsets in an order where every subset is reachable from smaller ones, so each of the 2^n states is computed once.',
    whenNotToUse: ['n exceeds about 22 — 2^n stops fitting', 'A count suffices instead of the identity of the set'],
    commonMistakes: ['Iterating masks in an order where a needed submask is not yet computed', 'Off-by-one between bit index and item index'],
    typicalTC: 'O(2^n · n)', typicalSC: 'O(2^n)',
    combinesWith: ['bitmask', 'graph-state-space'], googlePriority: true,
  },
  {
    id: 'dag-dp', topicId: 'advanced-dp', tier: 3,
    title: 'DP over a DAG',
    purpose: 'Memoise over graph nodes once acyclicity guarantees the recursion terminates.',
    subPatterns: ['longest increasing path in a matrix', 'longest path in a DAG', 'counting paths'],
    prerequisites: ['topological-sort', 'dp-1d'],
    recognitionSignals: ['A graph or grid where movement is strictly monotone in some quantity', '"Longest path" — NP-hard in general, tractable on a DAG', 'Repeated subpaths'],
    bruteForceSmell: 'DFS from every start without memoisation — exponential, re-exploring shared suffixes.',
    whyItWorks: 'Acyclicity means a node’s answer never depends on itself, so it can be computed once and cached.',
    whenNotToUse: ['The graph has cycles — memoisation will not terminate correctly', 'Edge weights are negative and cycles exist'],
    commonMistakes: ['Applying it to a cyclic graph', 'Not distinguishing "unvisited" from "computed as zero" in the memo table'],
    typicalTC: 'O(V + E)', typicalSC: 'O(V)',
    combinesWith: ['topological-sort', 'graph-traversal'], googlePriority: true,
  },

  /* ── Advanced graph algorithms ────────────────────────────── */
  {
    id: 'zero-one-bfs', topicId: 'advanced-graph-algorithms', tier: 3,
    title: '0-1 BFS with a deque',
    purpose: 'Get Dijkstra’s answer in linear time when every edge costs exactly 0 or 1.',
    subPatterns: ['minimum obstacle removal', 'minimum cost to make grid path valid'],
    prerequisites: ['dijkstra', 'monotonic-deque'],
    recognitionSignals: ['Weights are only 0 and 1', '"Minimum number of walls to break", "minimum flips"'],
    bruteForceSmell: 'Full Dijkstra with a heap — correct, but the log factor is unnecessary when there are only two edge costs.',
    whyItWorks: 'Push-front for a zero-cost edge and push-back for a one-cost edge keeps the deque sorted by distance without a heap.',
    whenNotToUse: ['Weights take more than two values', 'Weights are negative'],
    commonMistakes: ['Push-front and push-back reversed', 'Marking visited on push instead of pop'],
    typicalTC: 'O(V + E)', typicalSC: 'O(V)',
    combinesWith: ['dijkstra', 'graph-state-space'], googlePriority: true,
  },
  {
    id: 'mst', topicId: 'advanced-graph-algorithms', tier: 3,
    title: 'Minimum spanning tree',
    purpose: 'Connect every node at least total cost, by Kruskal with DSU or Prim with a heap.',
    subPatterns: ['Kruskal', 'Prim', 'min cost to connect all points', 'critical and pseudo-critical edges'],
    prerequisites: ['dsu', 'dijkstra'],
    recognitionSignals: ['"Connect all points at minimum cost"', 'A spanning structure is wanted, not a shortest path', 'The graph is undirected and weighted'],
    bruteForceSmell: 'Enumerating spanning trees — astronomically many, when the greedy cut property makes it a single sorted pass.',
    whyItWorks: 'The cut property: the lightest edge crossing any cut is in some MST, which is exactly what sorted-edge Kruskal exploits.',
    whenNotToUse: ['Shortest path between two nodes is wanted — that is Dijkstra and an MST does not give it', 'The graph is directed'],
    commonMistakes: ['Assuming MST paths are shortest paths — they are not', 'Forgetting to stop after V−1 edges'],
    typicalTC: 'O(E log E)', typicalSC: 'O(V)',
    combinesWith: ['dsu', 'sorting-greedy'], googlePriority: false,
  },
  {
    id: 'low-link', topicId: 'advanced-graph-algorithms', tier: 3,
    title: 'Low-link — bridges, articulation points and SCC',
    purpose: 'Use DFS discovery times and the earliest reachable ancestor to find structural weak points.',
    subPatterns: ['critical connections', 'articulation points', 'Tarjan SCC', 'Kosaraju'],
    prerequisites: ['graph-traversal', 'dag-dp'],
    recognitionSignals: ['"Critical connection", "single point of failure"', 'Strongly connected components in a directed graph', 'Removing one edge or node disconnects the graph'],
    bruteForceSmell: 'Removing each edge and re-running connectivity — O(E·(V+E)), when one DFS computes it all.',
    whyItWorks: 'An edge is a bridge exactly when the subtree below it can reach no ancestor above it — which low-link values capture in one pass.',
    whenNotToUse: ['A single connectivity question — plain DFS suffices', 'The graph is a tree, where every edge is trivially a bridge'],
    commonMistakes: ['Treating the parent edge as a back edge and corrupting low-link', 'Mishandling parallel edges'],
    typicalTC: 'O(V + E)', typicalSC: 'O(V)',
    combinesWith: ['graph-traversal'], googlePriority: false,
  },

  /* ── Advanced tree reasoning ──────────────────────────────── */
  {
    id: 'tree-rerooting', topicId: 'tree-dp', tier: 3,
    title: 'Rerooting',
    purpose: 'Compute an answer for every possible root in O(n) total, by reusing the parent’s result.',
    subPatterns: ['sum of distances in tree', 'tree with max score after removal'],
    prerequisites: ['tree-dp-intro'],
    recognitionSignals: ['The answer is wanted for EVERY node as root, not just one', '"Sum of distances from each node"'],
    bruteForceSmell: 'Running a full DFS from every node — O(n²), and adjacent roots differ by an easily derived delta.',
    whyItWorks: 'Two passes: one collects subtree aggregates upward; the second pushes the complementary "everything else" value downward.',
    whenNotToUse: ['Only one root matters', 'The aggregate cannot be inverted when moving the root'],
    commonMistakes: ['Deriving the child answer from the parent with the wrong sign or count', 'Mutating shared state between the two passes'],
    typicalTC: 'O(n)', typicalSC: 'O(n)',
    combinesWith: ['tree-dp-intro'], googlePriority: false,
  },

  /* ── Bit manipulation ─────────────────────────────────────── */
  {
    id: 'bitmask', topicId: 'bit-manipulation', tier: 3,
    title: 'Bit tricks and subset masks',
    purpose: 'Use XOR properties and masks to compress state or cancel pairs.',
    subPatterns: ['XOR cancellation', 'single number', 'counting bits', 'submask enumeration', 'maximum XOR'],
    prerequisites: [],
    recognitionSignals: [
      'Everything appears twice except one thing — XOR cancels pairs',
      'n around 20 with subsets — masks are the state',
      'O(1) space demanded on a counting problem',
    ],
    bruteForceSmell: 'A hash map of counts to find the unique element — correct, but O(n) space where XOR needs none.',
    whyItWorks: 'XOR is associative, commutative, and self-inverse, so duplicates annihilate regardless of order.',
    whenNotToUse: ['Elements repeat a number of times XOR does not cancel', 'Clarity matters more than the constant factor'],
    commonMistakes: ['Shifting by 32 or more in a 32-bit type — undefined behaviour', 'Forgetting that right shift on negatives is arithmetic in Java unless >>> is used'],
    typicalTC: 'O(n)', typicalSC: 'O(1)',
    combinesWith: ['dp-bitmask', 'trie-prefix'], googlePriority: true,
  },

  /* ── Advanced greedy ──────────────────────────────────────── */
  {
    id: 'exchange-argument', topicId: 'advanced-greedy', tier: 3,
    title: 'Justifying greedy with an exchange argument',
    purpose: 'Prove the greedy choice is safe by showing any optimal solution can be reshaped into it without loss.',
    subPatterns: ['scheduling to minimise lateness', 'sorting by a custom comparator', 'largest number from concatenation'],
    prerequisites: ['sorting-greedy'],
    recognitionSignals: ['A sort order is the crux and the comparator is non-obvious', '"Arrange to form the largest number"', 'Greedy looks right but you cannot say why'],
    bruteForceSmell: 'Trying permutations to confirm the ordering — factorial, and it verifies rather than explains.',
    whyItWorks: 'If swapping any adjacent out-of-order pair never worsens the objective, repeated swaps reach the greedy order from any optimum.',
    whenNotToUse: ['The exchange argument fails on a counterexample — then it is DP', 'The objective is not decomposable pairwise'],
    commonMistakes: ['A comparator that is not transitive, which is undefined behaviour in most sort implementations', 'Assuming greedy without testing a small counterexample'],
    typicalTC: 'O(n log n)', typicalSC: 'O(1)',
    combinesWith: ['sorting-greedy', 'heap-scheduling'], googlePriority: true,
  },

  /* ── Cross-pattern composition ────────────────────────────── */
  {
    id: 'composition-recognition', topicId: 'cross-pattern', tier: 3,
    title: 'Recognising a composition',
    purpose: 'Notice when a problem is two patterns fused, and identify which one supplies the outer loop.',
    subPatterns: [
      'hashmap + prefix sum', 'hashmap + sliding window', 'sorting + two pointers',
      'binary search + feasibility', 'tree + backtracking', 'graph + heap',
      'graph + DP', 'trie + backtracking', 'heap + streaming',
    ],
    prerequisites: ['prefix-hashmap', 'binary-search-answer', 'dag-dp', 'trie-prefix'],
    recognitionSignals: [
      'A familiar pattern gets you most of the way and then stalls on one sub-question',
      'The stall is itself a standard problem — that is the second pattern',
      'The problem resists a single label',
    ],
    bruteForceSmell: 'Hunting for the one correct label. Composition problems have no single label, and searching for one is the trap.',
    whyItWorks: 'Most Medium-Hard problems are one structural pattern supplying iteration and a second supplying a fast lookup or decision inside it. Naming the outer and inner roles separately makes them tractable.',
    whenNotToUse: ['The problem genuinely is a single pattern — do not manufacture complexity'],
    commonMistakes: [
      'Forcing the whole problem into the first recognised pattern and fighting the mismatch',
      'Solving the inner sub-question by brute force and losing the complexity gain the outer pattern bought',
    ],
    typicalTC: 'Depends on the composition',
    typicalSC: 'Usually dominated by the lookup structure',
    combinesWith: ['prefix-hashmap', 'sliding-window-frequency', 'dijkstra', 'dp-bitmask'],
    googlePriority: true,
  },
]
