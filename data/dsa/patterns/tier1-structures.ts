import type { Pattern } from '../types'

/** Tier 1 patterns over linked structures, trees, heaps, graphs, recursion, DP and greedy. */
export const tier1StructurePatterns: Pattern[] = [
  /* ── Linked List ──────────────────────────────────────────── */
  {
    id: 'linked-list-reverse', topicId: 'linked-list', tier: 1,
    title: 'Pointer rewiring',
    purpose: 'Relink nodes in place by holding exactly three references: previous, current and next.',
    subPatterns: ['full reverse', 'reverse sublist', 'reverse in k-groups', 'reorder list'],
    prerequisites: [],
    recognitionSignals: ['"Reverse", "reorder", "swap nodes" with O(1) space', 'The list must be modified rather than rebuilt'],
    bruteForceSmell: 'Copying values into an array, reversing, and writing back — O(n) space, and it sidesteps the pointer reasoning being tested.',
    whyItWorks: 'Saving `next` before overwriting `current.next` is sufficient to never lose the remainder of the list.',
    whenNotToUse: ['Values may be copied freely and clarity matters more than space', 'The structure is doubly linked and simpler operations exist'],
    commonMistakes: ['Overwriting current.next before saving it, orphaning the tail', 'Returning head instead of prev after the loop', 'Failing to reconnect the sublist boundaries on a partial reverse'],
    typicalTC: 'O(n)', typicalSC: 'O(1)',
    combinesWith: ['two-pointers-fast-slow'], googlePriority: false,
  },
  {
    id: 'linked-list-dummy', topicId: 'linked-list', tier: 1,
    title: 'Dummy head node',
    purpose: 'Remove head-versus-body special cases by prepending a sentinel you never return.',
    subPatterns: ['merge two lists', 'remove nth from end', 'partition list', 'delete duplicates'],
    prerequisites: ['linked-list-reverse'],
    recognitionSignals: ['The head itself may be deleted or replaced', 'Branching on "is this the first node?" is creeping into the code'],
    bruteForceSmell: 'Special-casing the head with a separate branch — it duplicates the loop body and is where the off-by-one bugs live.',
    whyItWorks: 'With a node guaranteed to precede every real node, every deletion and insertion has a predecessor, so one code path covers all positions.',
    whenNotToUse: ['The head is provably never modified'],
    commonMistakes: ['Returning dummy rather than dummy.next', 'Allocating the dummy inside the loop'],
    typicalTC: 'O(n)', typicalSC: 'O(1)',
    combinesWith: ['heap-topk'], googlePriority: false,
  },

  /* ── Stack / Queue / Deque ────────────────────────────────── */
  {
    id: 'stack-matching', topicId: 'stack-queue-deque', tier: 1,
    title: 'Stack for nesting and matching',
    purpose: 'Track the most recent unresolved item, because nesting resolves in last-in-first-out order.',
    subPatterns: ['valid parentheses', 'expression evaluation', 'path simplification', 'stack with auxiliary minimum'],
    prerequisites: [],
    recognitionSignals: ['Brackets, nesting, or undo semantics', '"Most recent unmatched", "innermost"'],
    bruteForceSmell: 'Repeatedly scanning for and deleting adjacent matching pairs — O(n²) for what one pass with a stack settles.',
    whyItWorks: 'Nesting is exactly LIFO: the pair that closes first is the one that opened most recently.',
    whenNotToUse: ['Order of resolution is first-in-first-out — use a queue', 'No nesting exists and a counter suffices'],
    commonMistakes: ['Popping an empty stack without checking', 'Not verifying the stack is empty at the end'],
    typicalTC: 'O(n)', typicalSC: 'O(n)',
    combinesWith: ['monotonic-stack'], googlePriority: false,
  },
  {
    id: 'monotonic-stack', topicId: 'stack-queue-deque', tier: 1,
    title: 'Monotonic stack',
    purpose: 'Keep the stack sorted so the next greater or smaller element is found in amortised O(1).',
    subPatterns: ['next greater', 'next smaller', 'previous greater', 'daily temperatures', 'stock span'],
    prerequisites: ['stack-matching'],
    recognitionSignals: ['"Next greater", "next smaller", "how many days until"', 'Each element needs a nearest neighbour satisfying a comparison'],
    bruteForceSmell: 'Scanning forward from every index for the next larger value — O(n²), and most of those scans re-walk the same tail.',
    whyItWorks: 'An element smaller than the incoming one can never be the answer for anything further right, so popping it discards nothing useful. Each element is pushed and popped once.',
    whenNotToUse: ['The comparison is not a simple ordering', 'You need the k-th greater rather than the next'],
    commonMistakes: ['Storing values when indices are needed for distance answers', 'Choosing the wrong strictness and mishandling equal neighbours'],
    typicalTC: 'O(n) amortised', typicalSC: 'O(n)',
    combinesWith: ['stack-matching'], googlePriority: true,
  },
  {
    id: 'monotonic-deque', topicId: 'stack-queue-deque', tier: 1,
    title: 'Monotonic deque',
    purpose: 'Maintain the maximum or minimum of a sliding window in O(1) per step.',
    subPatterns: ['sliding window maximum', 'bounded-range subarrays'],
    prerequisites: ['monotonic-stack', 'sliding-window-fixed'],
    recognitionSignals: ['A maximum or minimum is needed over a moving window', 'A heap would work but O(n log n) is too slow'],
    bruteForceSmell: 'Recomputing the window maximum on every slide — O(n·k).',
    whyItWorks: 'An element with a larger element to its right inside the window can never be the maximum again, so it can be discarded permanently.',
    whenNotToUse: ['The window is not contiguous', 'Both max and min plus arbitrary deletions are needed — use a balanced structure'],
    commonMistakes: ['Not evicting indices that have fallen outside the window', 'Storing values instead of indices, making eviction impossible'],
    typicalTC: 'O(n)', typicalSC: 'O(k)',
    combinesWith: ['sliding-window-fixed'], googlePriority: true,
  },
  {
    id: 'bfs-queue', topicId: 'stack-queue-deque', tier: 1,
    title: 'Queue-driven level expansion',
    purpose: 'Explore in order of distance, so the first time a node is reached is by a shortest path.',
    subPatterns: ['level order traversal', 'shortest path in an unweighted graph', 'multi-source BFS'],
    prerequisites: [],
    recognitionSignals: ['"Minimum number of steps", "shortest", "fewest moves"', 'All edges cost the same', 'Several starting points expand simultaneously'],
    bruteForceSmell: 'DFS exploring every path and keeping the shortest — exponential where BFS is linear.',
    whyItWorks: 'A queue visits nodes in non-decreasing distance order, so the first arrival at a node is optimal.',
    whenNotToUse: ['Edges have differing weights — use Dijkstra or 0-1 BFS', 'You need all paths rather than a shortest one'],
    commonMistakes: ['Marking visited on dequeue rather than enqueue, allowing duplicates into the queue', 'Losing the level boundary when depth is required'],
    typicalTC: 'O(V + E)', typicalSC: 'O(V)',
    combinesWith: ['graph-grid', 'tree-bfs'], googlePriority: true,
  },

  /* ── Trees ────────────────────────────────────────────────── */
  {
    id: 'tree-dfs', topicId: 'trees-bst', tier: 1,
    title: 'Recursive DFS with a returned value',
    purpose: 'Solve a tree problem by defining what each subtree returns to its parent.',
    subPatterns: ['depth', 'diameter', 'balanced check', 'root-to-leaf paths', 'subtree comparison'],
    prerequisites: [],
    recognitionSignals: ['The answer at a node depends on its subtrees', '"Height", "diameter", "path sum", "is balanced"'],
    bruteForceSmell: 'Recomputing subtree height at every node while checking balance — O(n²) because each height call re-walks the subtree.',
    whyItWorks: 'One post-order pass computes each subtree summary exactly once; the parent consumes children returns in O(1).',
    whenNotToUse: ['The traversal must be level by level — use BFS', 'The tree is deep enough to overflow the stack'],
    commonMistakes: [
      'Confusing what the function RETURNS with what it RECORDS — diameter returns height but records the through-path',
      'Missing the null base case',
      'Returning early and skipping the second subtree',
    ],
    typicalTC: 'O(n)', typicalSC: 'O(h) recursion stack, O(n) worst case',
    combinesWith: ['tree-bfs', 'backtracking-subsets'], googlePriority: true,
  },
  {
    id: 'tree-bfs', topicId: 'trees-bst', tier: 1,
    title: 'Level-order traversal',
    purpose: 'Process a tree one depth at a time when the answer is defined per level.',
    subPatterns: ['level order', 'right side view', 'zigzag', 'level averages', 'minimum depth'],
    prerequisites: ['bfs-queue'],
    recognitionSignals: ['"Level", "each row", "right side view", "minimum depth"'],
    bruteForceSmell: 'DFS collecting depth and value pairs then grouping — works, but loses the natural per-level boundary.',
    whyItWorks: 'Recording the queue length before draining a level gives exactly that level nodes.',
    whenNotToUse: ['The answer is a subtree aggregate — DFS is more direct'],
    commonMistakes: ['Reading the queue size inside the loop after it has changed', 'Enqueueing null children'],
    typicalTC: 'O(n)', typicalSC: 'O(w) widest level',
    combinesWith: ['bfs-queue'], googlePriority: true,
  },
  {
    id: 'bst-property', topicId: 'trees-bst', tier: 1,
    title: 'Exploiting the BST ordering invariant',
    purpose: 'Use left < node < right to prune half the tree, or to get sorted order from an in-order walk.',
    subPatterns: ['validate BST', 'search', 'insert and delete', 'kth smallest', 'LCA in a BST'],
    prerequisites: ['tree-dfs'],
    recognitionSignals: ['The input is stated to be a BST', '"Kth smallest", "closest value", "validate"'],
    bruteForceSmell: 'Treating the BST as an ordinary binary tree and scanning everything — discards the property the question handed you.',
    whyItWorks: 'In-order traversal of a BST is sorted, and comparing against the current node eliminates one subtree entirely.',
    whenNotToUse: ['The tree is not actually a BST', 'The tree is badly unbalanced, making O(h) equal O(n)'],
    commonMistakes: [
      'Validating with only a parent comparison instead of an inherited min/max range — the classic wrong answer',
      'Forgetting the duplicates policy',
    ],
    typicalTC: 'O(h) for search, O(n) for traversal', typicalSC: 'O(h)',
    combinesWith: ['tree-dfs'], googlePriority: true,
  },

  /* ── Heap ─────────────────────────────────────────────────── */
  {
    id: 'heap-topk', topicId: 'heap-pq', tier: 1,
    title: 'Bounded heap for Top-K',
    purpose: 'Keep only k candidates, so the cost is O(n log k) rather than a full sort.',
    subPatterns: ['kth largest', 'k closest', 'top k frequent', 'k-way merge'],
    prerequisites: [],
    recognitionSignals: ['"Top k", "kth largest", "k closest"', 'k is much smaller than n', 'A stream where the full input cannot be held'],
    bruteForceSmell: 'Sorting everything to read k elements — O(n log n) to answer a question about k items.',
    whyItWorks: 'A min-heap of size k has the kth largest at its root; anything smaller can be rejected in O(1).',
    whenNotToUse: ['k is close to n — sorting is simpler and comparable', 'Counting sort or bucketing applies and gives O(n)'],
    commonMistakes: ['Using a max-heap for kth largest and losing the O(log k) bound', 'Comparator inverted, silently returning the k smallest'],
    typicalTC: 'O(n log k)', typicalSC: 'O(k)',
    combinesWith: ['hash-frequency', 'linked-list-dummy'], googlePriority: true,
  },

  /* ── Graph ────────────────────────────────────────────────── */
  {
    id: 'graph-grid', topicId: 'graph-core', tier: 1,
    title: 'Grid as an implicit graph',
    purpose: 'Treat cells as nodes and adjacency as edges, without ever materialising an adjacency list.',
    subPatterns: ['islands', 'max area', 'flood fill', 'rotting oranges', 'boundary-anchored regions'],
    prerequisites: ['bfs-queue'],
    recognitionSignals: ['A 2D grid with connected regions', '"Islands", "regions", "spread", "surrounded"'],
    bruteForceSmell: 'Building an explicit adjacency list for m·n cells — pure overhead when neighbours are computable arithmetically.',
    whyItWorks: 'A cell neighbours are the four adjacent coordinates; the graph is implied by the indices themselves.',
    whenNotToUse: ['Connectivity is not spatial', 'Repeated connectivity queries after merges — use DSU'],
    commonMistakes: [
      'Not marking visited at enqueue time, revisiting cells and blowing up the queue',
      'Missing the bounds check before reading a neighbour',
      'Mutating the input grid when the caller needed it intact',
    ],
    typicalTC: 'O(m·n)', typicalSC: 'O(m·n)',
    combinesWith: ['bfs-queue', 'backtracking-grid'], googlePriority: true,
  },
  {
    id: 'graph-traversal', topicId: 'graph-core', tier: 1,
    title: 'Graph DFS and BFS over an adjacency list',
    purpose: 'Visit every reachable node once, with an explicit visited set.',
    subPatterns: ['connected components', 'clone graph', 'cycle detection in undirected graphs', 'reachability'],
    prerequisites: ['bfs-queue'],
    recognitionSignals: ['Explicit edges, or pairs that imply relationships', '"Connected", "components", "can reach"'],
    bruteForceSmell: 'Repeated pairwise reachability checks — each one re-walks the graph.',
    whyItWorks: 'A visited set makes every node and edge cost O(1) once, giving O(V + E) overall.',
    whenNotToUse: ['Edges are weighted and shortest paths matter', 'The graph is dynamic — use DSU'],
    commonMistakes: ['Forgetting the parent check when detecting cycles in an undirected graph', 'Not iterating all nodes, missing disconnected components'],
    typicalTC: 'O(V + E)', typicalSC: 'O(V)',
    combinesWith: ['graph-grid'], googlePriority: true,
  },

  /* ── Recursion / Backtracking ─────────────────────────────── */
  {
    id: 'backtracking-subsets', topicId: 'recursion-backtracking', tier: 1,
    title: 'Choose, explore, un-choose',
    purpose: 'Enumerate combinatorial structures by making a choice, recursing, then undoing it.',
    subPatterns: ['subsets', 'permutations', 'combinations', 'combination sum'],
    prerequisites: [],
    recognitionSignals: ['"All possible", "every combination", "return all"', 'n is small — roughly 20 or fewer, hinting at exponential output'],
    bruteForceSmell: 'Iterative construction with nested loops — impossible to write when the depth is not known ahead of time.',
    whyItWorks: 'The recursion tree enumerates every choice sequence exactly once; undoing after the call restores state for the sibling branch.',
    whenNotToUse: ['Only the COUNT is needed — that is usually DP', 'n is large enough that exponential output is impossible'],
    commonMistakes: [
      'Adding the path by reference instead of copying, so every result mutates to empty',
      'Forgetting to un-choose, leaking state into sibling branches',
      'Handling duplicates by sorting but then not skipping equal siblings',
    ],
    typicalTC: 'O(n · 2^n) for subsets, O(n · n!) for permutations', typicalSC: 'O(n) recursion depth excluding output',
    combinesWith: ['tree-dfs', 'backtracking-grid'], googlePriority: true,
  },
  {
    id: 'backtracking-grid', topicId: 'recursion-backtracking', tier: 1,
    title: 'Backtracking over a grid',
    purpose: 'Search paths through a grid, marking cells on the way in and restoring them on the way out.',
    subPatterns: ['word search', 'path existence', 'constrained placement'],
    prerequisites: ['backtracking-subsets', 'graph-grid'],
    recognitionSignals: ['A path through a grid must satisfy a sequence constraint', 'A cell may be reused by a different path but not within one path'],
    bruteForceSmell: 'Enumerating all paths without pruning — the search space explodes without early exit on mismatch.',
    whyItWorks: 'Marking the cell prevents reuse within the current path; restoring it keeps other paths available.',
    whenNotToUse: ['Any visit is permanent — plain DFS is enough', 'Many words are searched — use a Trie'],
    commonMistakes: ['Forgetting to restore the cell after the recursive call', 'Checking bounds after indexing rather than before'],
    typicalTC: 'O(m·n·4^L)', typicalSC: 'O(L)',
    combinesWith: ['graph-grid'], googlePriority: true,
  },

  /* ── DP core ──────────────────────────────────────────────── */
  {
    id: 'dp-1d', topicId: 'dp-core', tier: 1,
    title: '1D dynamic programming',
    purpose: 'Define state over one index, decide the choice at that index, and build up from base cases.',
    subPatterns: ['climbing stairs', 'house robber', 'decode ways', 'coin change', 'word break'],
    prerequisites: ['backtracking-subsets'],
    recognitionSignals: [
      'Count the ways, or optimise over a sequence of choices',
      'Overlapping subproblems — the same index is recomputed by different branches',
      'A greedy choice is demonstrably wrong on a small counterexample',
    ],
    bruteForceSmell: 'Plain recursion recomputing the same index exponentially — the recursion tree has repeated nodes.',
    whyItWorks: 'Optimal substructure plus overlapping subproblems means each state is computed once and reused.',
    whenNotToUse: ['A greedy choice is provably optimal', 'States do not overlap — plain recursion is fine'],
    commonMistakes: [
      'Defining state ambiguously, so the transition cannot be written cleanly',
      'Wrong base case, especially dp[0] versus dp[1]',
      'Using an unreachable sentinel like the max integer and overflowing on addition',
    ],
    typicalTC: 'O(n · choices)', typicalSC: 'O(n), often reducible to O(1)',
    combinesWith: ['dp-grid'], googlePriority: true,
  },
  {
    id: 'dp-grid', topicId: 'dp-core', tier: 1,
    title: 'Grid dynamic programming',
    purpose: 'Define state over two indices when the problem walks a 2D table.',
    subPatterns: ['unique paths', 'minimum path sum', 'grid with obstacles'],
    prerequisites: ['dp-1d'],
    recognitionSignals: ['Movement restricted to right and down', 'Count paths or minimise a cost across a grid'],
    bruteForceSmell: 'Recursing over every path — exponential where the table is O(m·n).',
    whyItWorks: 'Each cell depends only on already-computed neighbours, so a single sweep in the right order fills the table.',
    whenNotToUse: ['Movement can revisit cells — that is a graph search, not DP', 'The grid is enormous and only one path is needed'],
    commonMistakes: ['Initialising the first row and column incorrectly', 'Optimising to one row before the transition is correct'],
    typicalTC: 'O(m·n)', typicalSC: 'O(m·n), reducible to O(n)',
    combinesWith: ['dp-1d'], googlePriority: true,
  },

  /* ── Greedy / Intervals ──────────────────────────────────── */
  {
    id: 'sorting-greedy', topicId: 'greedy-intervals', tier: 1,
    title: 'Sort, then take the locally best choice',
    purpose: 'Impose an order that makes the greedy choice provably safe.',
    subPatterns: ['jump game', 'gas station', 'assign by size', 'scheduling by end time'],
    prerequisites: [],
    recognitionSignals: ['Maximise or minimise a count where a natural ordering exists', 'A local rule seems to work and no counterexample appears'],
    bruteForceSmell: 'Trying all orderings or all subsets — factorial or exponential for something one sorted pass settles.',
    whyItWorks: 'After sorting, an exchange argument shows any optimal solution can be transformed into the greedy one without getting worse.',
    whenNotToUse: [
      'A counterexample to the local rule exists — then it is DP',
      'The choice at one step changes the feasibility of far-future steps in a way sorting does not capture',
    ],
    commonMistakes: ['Sorting by the wrong key — start time instead of end time for scheduling is the classic error', 'Assuming greedy works without testing a counterexample'],
    typicalTC: 'O(n log n)', typicalSC: 'O(1) beyond the sort',
    combinesWith: ['two-pointers-sorted', 'intervals-merge'], googlePriority: true,
  },
  {
    id: 'intervals-merge', topicId: 'greedy-intervals', tier: 1,
    title: 'Interval sweep',
    purpose: 'Sort intervals by an endpoint, then sweep once maintaining the current active interval.',
    subPatterns: ['merge intervals', 'insert interval', 'non-overlapping removal', 'meeting rooms'],
    prerequisites: ['sorting-greedy'],
    recognitionSignals: ['Input is pairs of start and end', '"Overlap", "merge", "how many rooms", "remove the fewest"'],
    bruteForceSmell: 'Comparing every interval against every other — O(n²), and unsorted comparison cannot decide transitivity cheaply.',
    whyItWorks: 'Once sorted by start, any interval overlapping the current one must begin before the current one ends, so a single running interval suffices.',
    whenNotToUse: ['Intervals are on a circle', 'Arrivals are streaming and cannot be sorted upfront'],
    commonMistakes: [
      'Sorting by start when the problem needs end-time ordering to minimise removals',
      'Getting the touching-boundary policy wrong: does [1,2] overlap [2,3]?',
    ],
    typicalTC: 'O(n log n)', typicalSC: 'O(n) for output',
    combinesWith: ['sorting-greedy', 'heap-topk'], googlePriority: true,
  },
]
